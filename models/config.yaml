model_type: "oh"  # Options: "oh" (one-hot), "emb" (embedding)

dataset_settings:              
  max_protein_length: 1000
  num_samples: 10000

# Ignore settings above until final dataset is ready

model_settings:
  cnn_dim: 256
  dropout: 0.1    # Not implemented yet #TODO
  dilation: 2
  kernel_size: 5

train_settings:
  batch_size: 128
  epochs: 50
  learning_rate: 0.00004
  lr_decay: 0.95

  seed: 42
